server:
  forward-headers-strategy: native

spring:
  application:
    name: tradex-server

  datasource:
    url: jdbc:postgresql://localhost:5432/tradex_test
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: test-google-client-id
            client-secret: test-google-client-secret
            scope:
              - email
              - profile
          kakao:
            client-id: test-kakao-client-id
            client-secret: test-kakao-client-secret
            client-authentication-method: client_secret_post
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            scope:
              - profile_nickname
              - profile_image
              - account_email
            client-name: Kakao
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id

  mail:
    host: smtp.gmail.com
    port: 587
    username: test@example.com
    password: test-password
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  data:
    redis:
      host: localhost
      port: 6379

  ai:
    openai:
      api-key: test-api-key
      chat:
        options:
          model: gpt-4o-mini
          temperature: 0.7
          max-tokens: 2000

jwt:
  secret: dGhpc2lzYXZlcnlsb25nc2VjcmV0a2V5Zm9yand0dG9rZW5nZW5lcmF0aW9uMTIzNDU2Nzg5MA==
  access-token-expiration: 900000
  refresh-token-expiration: 604800000

solapi:
  api-key: test-solapi-api-key
  api-secret: test-solapi-api-secret
  sender-number: 010-0000-0000

app:
  oauth2:
    redirect-uri: http://localhost:3000/oauth2/redirect
    allowed-redirect-uris:
      - http://localhost:3000
  cors:
    allowed-origins: http://localhost:3000
  password-reset:
    base-url: http://localhost:3000/reset-password

encryption:
  secret-key: P/V73TTVO9+F2ZLi9P/mjzrdnA8ScN+8OTq7Z4Ev+Zs=
